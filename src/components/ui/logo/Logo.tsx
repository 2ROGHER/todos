/**
 * This component is responsible for rendering the Logo component in the navigation bar section.
 */

import React, { JSX } from "react";

export const Logo = (): JSX.Element => (<div className="logo"> SVG LOGO</div>);
